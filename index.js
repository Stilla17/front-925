// setTimeout(() => {
//     console.log("Sunnatilla");
// }, 1000); 
document.addEventListener('DOMContentLoaded', function () {
    // Код, который должен выполниться после полной загрузки DOM
    console.log("DOM fully loaded.");

    setTimeout(() => {
        console.log("Sunnatilla");
    }, 1000);
});